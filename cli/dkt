#!/usr/bin/env node

const program = require('commander')
const pkg = require('../package.json')


program
  .version(pkg.version)
  .command('lambda [command]', 'build, bundle, deploy functions')
  .command('stepfunctions [command]', 'deploy, start step functions')
  .command('test', 'run tests')
  .parse(process.argv)
